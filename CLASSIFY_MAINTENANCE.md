# 分类维护手册（永久待办 · 随时办）

> **任务性质**：打磨飞书消息分类是一个**永久的、随时办的待办**。
> 分类只服务于当前工作内容：现在是程序开发为主，以后会涉及**商业、法律、知识学习**等各式各样，
> 每次遇到"这消息分错类了 / 该有新分类了"，就随时来改——改完在下方记录一笔即可。

## 分类机制速览

- 分类代码：`dsh-plugin-feishu/lib/classify-rules.js`（**唯一的活规则文件**）
- 分类逻辑：`dsh-plugin-feishu/lib/index.js` 的 `classifyTask()` —— 只负责按顺序套规则，不写死任何关键词
- 每个分类 = 一个独立会话 `feishu-<分类>-<日期>`，同类任务复用上下文、异类任务互相隔离
- 兜底分类 `chat`（闲聊/日常），未命中任何规则时使用

## 当前分类清单（2026-08-18 更新）

| 分类 | 说明 | 典型消息 |
|---|---|---|
| `dev` | 程序开发 / 代码 | 写个脚本、修 bug、重构、部署、git 操作 |
| `sys` | 系统 / 运维 | 服务状态、日志、安装配置、端口网络、备份 |
| `business` | 商业 / 经营 / 商务 | 报价、合同、客户销售、财务成本、融资 |
| `legal` | 法律 / 合规 | 法律条款、诉讼仲裁、知识产权、纠纷 |
| `study` | 知识学习 / 阅读 / 笔记 | 读书笔记、课程、概念讲解、考试 |
| `query` | 通用查询 / 资料检索 | 查一下、总结、分析、解释、搜索 |
| `feishu` | 飞书 / 桥接 / 插件 | 飞书消息、插件配置、机器人 |
| `chat`（兜底） | 闲聊 / 日常 | 打招呼、心情、琐事 |

## 如何新增 / 调整分类（3 步，5 分钟）

1. **编辑** `lib/classify-rules.js` 的 `rules` 数组：加一条 `{ category, desc, patterns }`，
   或改现有条目的关键词。
   - 关键词原则：**精确短语优先，宽泛单词慎用**。
     用 `写代码|写脚本` 而不是 `写`（否则"写一份商业合同"会误入 dev）。
   - 顺序原则：**具体领域靠前，通用查询靠后**（先命中先得）。
2. **记一笔**：在下方"变更记录"追加一行（日期、改了什么、为什么）。
3. **重启生效**：改动 `classify-rules.js` 后无需改其他文件，插件重启即生效；
   新分类第一次用到时会自动创建它的独立会话。

> 小提示：如果某类消息**老被分错**，多半是该分类的关键词太宽/太窄——把误判的关键词从
> 那个分类移除，或加进正确的分类即可。

## 变更记录

| 日期 | 变更内容 | 原因 |
|---|---|---|
| 2026-08-18 | 分类规则文件化（classify-rules.js）；新增 `business`/`legal`/`study`；feishu 提到最前；dev 关键词去宽泛化（"写"→"写.{0,12}脚本"）；"合同"商务语境词留 business、裸"合同"归 legal；legal 补"条文/劳动法" | 用户明确：工作将涉及商业/法律/知识学习，分类要随时按工作内容扩展 |
| 2026-08-18 | 初始硬编码分类 dev/sys/query/feishu/chat（index.js 内联正则） | 插件初版 |

## 待办打磨点（想到就记，随时清）

- [ ] 等真实商业/法律消息出现后，回头校准 business/legal 的关键词边界（如"合同"归商务还是法律）
- [ ] 观察 query 是否过宽（"总结/分析/对比"太泛，可能吃掉领域消息）
- [ ] 以后工作出现新领域（如人力资源、项目管理、数据分析）时，随手新增分类
